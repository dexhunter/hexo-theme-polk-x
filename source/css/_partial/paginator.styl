.page-nav {
  padding: 0 20px;

  .prev,
  .page-number {
    padding: 10px 20px 10px 0;
  }

  .next {
    float: right;
  }

  a {
    color: var(--gray-color);

    &:hover {
      color: var(--link-hover-color);
    }
  }
}